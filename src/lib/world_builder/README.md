# world_builder

Owns: text ingestion, chunking, world graph extraction, lorebook + timeline
construction. The current implementation lives in `../parser-client.ts` and
will be moved here in **Phase 2** when the world graph gets first-class
`Faction` / `LoreEntry` / `Conflict` outputs.
