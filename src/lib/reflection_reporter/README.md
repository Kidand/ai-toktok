# reflection_reporter

Owns: in-flight scene reflection (`generateSceneReflection`) and end-of-game
epilogue. The epilogue path already lives in `../narrator-browser.ts`; the
in-flight reflection is new in Phase 6 and uses the prompt at
`../prompts/reflection.ts`.
