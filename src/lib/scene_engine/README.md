# scene_engine

Owns: `Scene` lifecycle — opening scene creation, present-agent tracking,
state advancement. Phase 3 introduces the Scene entity (replacing today's
"recent N narrative entries" presence heuristic in `play/page.tsx`).
